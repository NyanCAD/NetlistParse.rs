[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "netlist_parser"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
